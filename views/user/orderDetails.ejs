<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order Details</title>
    <style>
        body {
  font-family: Arial, sans-serif;
  font-size: 16px;
  margin: 0;
  padding: 20px;
}

main {
  display: flex;
  flex-direction: column;
  align-items: center;
  max-width: 800px;
  margin: 0 auto;
}

h1, h2 {
  margin-bottom: 20px;
}

table {
  width: 100%;
  border-collapse: collapse;
  margin-bottom: 40px;
}

th, td {
  padding: 10px;
  text-align: left;
  border: 1px solid #ccc;
}

th {
  background-color: #f2f2f2;
}

img {
  width: 100px;
  height: 100px;
  object-fit: cover;
}

.buttons {
  display: flex;
  justify-content: space-between;
  margin-top: 20px;

}
p{
    margin:5px 1px;
}
a {
  text-decoration: none;
  padding: 10px 20px;
  background-color: #4CAF50;
  color: white;
  border-radius: 5px;
  margin: 0px 5px;
}

    </style>
</head>
<body>
    <main >
        <h1>Order Details</h1>

        <table>
            <thead>
                <tr>
                    <th>Product Name</th>
                    <th>Price</th>
                    <th>Quantity</th>
                </tr>
            </thead>
            <tbody>
               <% if((orders.products).length>0) %>
                <% (orders.products).forEach(function(orderDetail,index) { %>

                <tr>
                   
                    <td><%= orders.products[index].product  %></td>
                    <td><%= orders.products[index].price %></td>
                    <td><%= orders.products[index].quantity %></td>
                </tr>
                <% }); %>
                </tbody>
        </table>

        <h2>Address</h2>
        <p>Name:<%= orders.username %></p>
        
        <p><%= orders.address.address %></p>
        <p><%= orders.address.city %></p>
        <p><%= orders.address.state %></p>
        <p><%= orders.address.pincode %></p>

        <td><%= orders.orderDate %></td>

        <p style="font-size: 1.5em;"><strong>Total Purchase Amount: ₹<%= orders.totalPrice %></strong></p>

        <div class="buttons">
            <a href="/home">Continue Shopping</a>
            <a href="/user/generateInvoice/<%= orders._id %>" class="btn btn-primary mt-5">Download Invoice</a>
          </div>
    </main>

    </body>
</html>
